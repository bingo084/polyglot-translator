export com.bingo.polyglot.core.entity.TranslateTask

specification TranslateTaskSpec {
  status
  associatedIdEq(sourceAudio)
  ge(createTime)
  le(createTime)
  ge(finishTime)
  le(finishTime)
}

input TranslateTaskInput {
  id(sourceAudio)
  originalText
  targetLanguage
}
